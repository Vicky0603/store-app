# Server config
server.port=8081

# Datasource (configure per environment)
spring.datasource.url=jdbc:mariadb://127.0.0.1:3306/user_service
spring.datasource.username=root
spring.datasource.password=test1234
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT config (use strong secret in env)
security.jwt.secret=ThisIsAStrongJWTSecretWithAtLeast32Chars_2026!
security.jwt.expiration=3600000
